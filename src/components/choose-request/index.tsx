import { FunctionComponent } from "react"
import { Collection } from '@/types/postman/collection'

type ChooseRequestProps = {
  collection: Collection
}

export const ChooseRequest: FunctionComponent<ChooseRequestProps> = () => {
  return null
}
